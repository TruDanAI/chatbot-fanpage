# demo-shop fixture

`demo-shop` is a fake, test-only shop used to boot staging with `SHOP_ID=demo-shop`.
It must not contain real page IDs, tokens, customer data, production data, or copied
shop-specific product text/assets.
